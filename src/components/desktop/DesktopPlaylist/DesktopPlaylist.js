import React from 'react';

import DesktopSong from '../../desktop/DesktopSong/DesktopSong'

function DesktopPlaylist(props) {

    const playlistSongs = props.tracks.map( (track, i) => {
        
    })
    return(
        <div>

        </div>
    )
}

export default DesktopPlaylist;